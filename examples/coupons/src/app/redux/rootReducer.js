// ** Reducers Imports
import navbar from './navbar';
import layout from './layout';
import invoice from '@app/views/apps/invoice/store';

const rootReducer = {
    navbar,
    layout,
    invoice,
};

export default rootReducer;
